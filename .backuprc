#home prefix for relative paths
prefix=$HOME

#format for date
format='%m_%d_%Y'

#Path to the directory where backups will be stored
backup_dir="$home_prefix/.backups"


#paths
paths=(
        "$prefix/Documents" \
        "$prefix/.backuprc" \
        "$prefix/.bashrc" \
        "$prefix/.vimrc" \
        "/usr/share/backgrounds"
        )






























